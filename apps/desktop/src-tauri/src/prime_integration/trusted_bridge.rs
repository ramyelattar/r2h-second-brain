#![cfg(any())]

//! Archived legacy ELE bridge source boundary.
//!
//! This file is intentionally never declared by `prime_integration/mod.rs`.
//! It remains as a rollback marker only; the active provider is the direct
//! ProgramData-backed generation adapter in `local_ai_client.rs`.
